meta {
  name: Update ST
  type: http
  seq: 4
}

post {
  url: {{host}}setType/2
  body: multipartForm
  auth: bearer
}

headers {
  Content-Type: multipart/form-data
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  name: Recibo
}
