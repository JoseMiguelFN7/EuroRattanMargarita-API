meta {
  name: Update Unit
  type: http
  seq: 4
}

post {
  url: {{host}}unit/2
  body: multipartForm
  auth: bearer
}

headers {
  Content-Type: multipart/form-data
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  name: Caja
}
