meta {
  name: Create MT
  type: http
  seq: 3
}

post {
  url: {{host}}materialType
  body: multipartForm
  auth: bearer
}

headers {
  Content-Type: multipart/form-data
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  name: Cierre
}
